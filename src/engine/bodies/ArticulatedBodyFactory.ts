/* eslint-disable @typescript-eslint/no-explicit-any */
import { getRapier } from "@/engine/physics/rapierRuntime";
import type { PhysicsRuntime } from "@/engine/physics/PhysicsRuntime";
export type PartDefinition<PartId extends string>={id:PartId;parent?:PartId;position:[number,number,number];shape:"box"|"sphere"|"capsule";size:[number,number,number];ballast?:boolean};
export class ArticulatedBody<PartId extends string>{constructor(readonly parts:Map<PartId,{body:any;collider:any;parent?:PartId}>){ } dispose(runtime:PhysicsRuntime):void{this.parts.forEach(({body,collider})=>{runtime.world.removeCollider(collider,true);runtime.world.removeRigidBody(body);});this.parts.clear();}removeSubtree(id:PartId,runtime:PhysicsRuntime):void{const children=[...this.parts.entries()].filter(([,part])=>part.parent===id).map(([child])=>child);children.forEach((child)=>this.removeSubtree(child,runtime));const part=this.parts.get(id);if(part){runtime.world.removeCollider(part.collider,true);runtime.world.removeRigidBody(part.body);this.parts.delete(id);}}}
export class ArticulatedBodyFactory<PartId extends string>{constructor(private readonly runtime:PhysicsRuntime){} create(parts:readonly PartDefinition<PartId>[]):ArticulatedBody<PartId>{const rapier=getRapier();const output=new Map<PartId,{body:any;collider:any;parent?:PartId}>();for(const part of parts){if(output.has(part.id))throw new Error(`Duplicate part: ${part.id}`);const descriptor=rapier.RigidBodyDesc.dynamic().setTranslation(...part.position);const body=this.runtime.world.createRigidBody(descriptor);const collider=part.shape==="box"?rapier.ColliderDesc.cuboid(...part.size):part.shape==="sphere"?rapier.ColliderDesc.ball(part.size[0]):rapier.ColliderDesc.capsule(part.size[1],part.size[0]);if(part.ballast)collider.setDensity(3);const created=this.runtime.world.createCollider(collider,body);output.set(part.id,{body,collider:created,parent:part.parent});}return new ArticulatedBody(output);}}
